- quickfix db lock
/ web adapter
/ manual
- document service interfaces

- fix duplicate elimination
	- add primary key to ebms_message table
	- use key in other tables
- write mime message
	- add encryption (s/mime)
- define DeliveryChannel types (http, etc.)

- optimize EbMSMessageutils and CPAUtils
	- implement CPARegistry and CPA object
- change events
	- create only one event
	- add expiration time and retry attempt nr to event
	- db locking issue
		- add events Acknowledgment and MessageError
		- add priority to events and event_queue

nice to have features:
- add method getPartyIds(cpaId,role) to EbMSMessageService
- add endpoint type support (now only allPurpose endpoint type is supported):
	- add messageType to MessageContext to determine endpoint type to use
	- parse incoming url to determine endpoint type used
- add ackRequested (and ackSignatureRequested) perMessage support (now if ackRequested is perMessage, then no acknowledgment is requested)
	- add ackRequested (and ackSignatureRequested) to MessageContext to determine if request is needs ack
- add duplicateElimination perMessage support (now if duplicateElimination is perMessage, then no duplicateElimination is requested)
	- add duplicateElimination to MessageContext to determine if request is needs duplicateElimination
- improve Manifest validation and Attachment storage:
	- allow url as href?
		- store url as attachment
	- store optional manifest description and schema with attachment???
- implement Packaging
- use PersistDuration to remove messages

- remove hard coded character encoding (UTF-8)?

2.5:
/ implement push interface with jms
/ implement push soap interface?
- implement events with quartz or jms
/ implement deliveryManager with jms
- implement security on soap interface

2.x:
- implement SSLFactoryManager that returns a CPA/hostname related SSLSocketFactory to handle different key/certificate pairs
	- load right SSLFactoryManager on each URLConnection request

- make EbMSMessageProcessor event driven
	- implement eventManager
	- replace dao.insertSendEvent(messageId) (and/or EbMSMessageUtils.getEbMSSendEvents(cpa,message)) to eventManager.generateSendEvent(EbMSDocument)
	- implement eventManager.generateReceiveEvent(MimeMessage/EbMSDocument)

- let DeliveryManager work with EbMSDocument instead of EbMSMessage?
	- change messageQueue from EbMSMessage to EbMSDocument
	- get messageId from EbMSDocument

3.0:
- validate requested uri

- implement EbMSFileAttachment (file proxy) and EbMSDBAttachment (db proxy) for large messages?

- implement business signals handling (ReceiptAcknowledgment, AcceptanceAcknowledgment, and Exception)
- implement business response messages handling

- replace custom message writer by mime4j???
- replace xmlsec by bouncycastle???